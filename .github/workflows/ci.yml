name: Gradle CI/CD Pipeline

on:
  push:
    branches: [ "tasks/USR-0", "main" ]
  pull_request:
    branches: [ "tasks/USR-0", "main" ]

jobs:

  ci-build-test:
    name: CI - Build & Test
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: test
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
        ports:
          - 5439:5432
        options: >-
          --health-cmd="pg_isready"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd="redis-cli ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build project (without tests)
        run: ./gradlew clean build -x test

      - name: Run tests and coverage
        run: |
          ./gradlew test jacocoTestReport

      - name: SonarCloud Scan
        run: |
          ./gradlew sonar \
            -Dsonar.projectKey=YauhenNohach_innowise_internship \
            -Dsonar.organization=yauhennohach \
            -Dsonar.host.url=https://sonarcloud.io \
            -Dsonar.login=${{ secrets.SONAR_TOKEN }}

      - name: Upload JAR artifacts
        uses: actions/upload-artifact@v4
        with:
          name: jar-artifacts
          path: |
            */build/libs/*.jar
            !build/libs/
          retention-days: 7

  cd-dev:
    needs: ci-build-test
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/dev'

    steps:
      - uses: actions/checkout@v4

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: jar-artifacts

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker images (dev)
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/userservice:dev-latest -t ${{ secrets.DOCKER_USERNAME }}/userservice:${{ github.sha }} -f userservice/Dockerfile .
          docker push ${{ secrets.DOCKER_USERNAME }}/userservice:dev-latest
          docker push ${{ secrets.DOCKER_USERNAME }}/userservice:${{ github.sha }}

      - name: Deploy to Dev environment
        run: echo "Deploying to dev environment..."